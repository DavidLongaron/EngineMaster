#version 460
out vec4 FragColor;
in vec3 ourColor;
in vec2 TexCoord;

uniform layout(binding=0) sampler2D texture1;
uniform layout(binding=1) sampler2D texture2;

void main()
{
//FragColor = vec4(ourColor, 1.0f);
FragColor = mix(texture(texture1, TexCoord), texture(texture2, TexCoord), 0.3);
}